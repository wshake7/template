version: v2

modules:
  - path: protos
    lint:
      use:
        - STANDARD
    breaking:
      use:
        - FILE
deps:
  - 'buf.build/gnostic/gnostic'

breaking:
  use:
    - FILE

lint:
  use:
    - DEFAULT

# 2026-05-19

- Fixed `admin-react` dev server import resolution after shared package extraction:
  - Added explicit Vite `resolve.alias` entries for `@vp/build-config`, `@vp/core`, `@vp/react-core`, `@vp/request`, and `@vp/utils` in all three React app configs.
  - This lets workspace package source imports resolve to `front/packages/*/src` during dev without requiring prebuilt `dist` outputs.
  - Verified `vp run admin-react#build`.
  - Verified the dev server can transform the original failing module `front/packages/react-core/src/mock/helpers.ts` and rewrite `@vp/core` to the core source entry.
  - Ran `vp staged`.
- Removed deprecated TypeScript `compilerOptions.baseUrl` from `admin-react`, `app-react`, and `app-react-ssr` tsconfig files:
  - Kept existing `paths` mappings because they already use explicit relative targets, so no `ignoreDeprecations` workaround is needed.
  - Verified `vp run admin-react#build`, `vp run app-react#build`, and `vp run app-react-ssr#build`.

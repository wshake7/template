# 2026-05-12

- 前端 `admin-react` tabs 栏新增右键菜单，支持“刷新 / 关闭 / 全部关闭”；刷新通过递增缓存面板版本强制重挂载，关闭非当前页签时不再自动跳转。验证 `vp run admin-react#build`、`vp staged` 通过。
- 调整 tabs 关闭行为：关闭最后一个当前页签时允许关闭，并导航到 `/`；全部关闭也保持跳转到 `/`。验证 `vp run admin-react#build`、`vp staged` 通过。
- tabs 右键菜单补充“新窗口打开”，基于当前 origin 和页签路径打开新浏览器窗口，不影响当前页签状态。验证 `vp run admin-react#build`、`vp staged` 通过。
- API 日志列表新增“操作平台”和“地理位置”列，操作平台按 OS + BrowserName 展示，并兼容后端 `oSName/oSVersion` 字段。验证 `vp run admin-react#build`、`vp staged` 通过。
- API 日志列表新增“请求ID”列，直接展示 `requestID` 并开启省略显示。验证 `vp run admin-react#build`、`vp staged` 通过。
- 前端 `admin-react` 路由菜单改为完全依赖 `/api/sys/resource/menu/tree` 返回的动态菜单：移除所有路由 `staticData.menu`、`_app.tsx` 中的 `staticMenuItems` / `buildMenuTree` 静态兜底和 `route.options.staticData` tab 标题回退。
- 删除静态菜单构建工具 `front/apps/admin-react/src/utils/menu.ts`，同步清理 router 的 `StaticDataRouteOption` 扩展和自动导入声明。
- 验证 `vp run admin-react#build` 通过；`vp staged` 仍因 `front/vite.config.ts` 缺少 `staged` 配置失败。
- 修复后端动态菜单树 `/api/sys/resource/menu/tree` 未按角色菜单授权过滤的问题：现在根据当前 session 的 `RoleIDs` 查询 `sys_role_menu`，只返回授权菜单及其父级链。验证 `go fix ./...`、`go vet ./...`、`go test ./...` 通过。

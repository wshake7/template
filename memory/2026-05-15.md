# 2026-05-15

- 修复 admin-react 用户管理页编辑保存未触发请求的问题：保存按钮改为显式校验表单并调用提交逻辑，同时编辑更新 payload 保留 `isEnabled` 状态；已通过 `vp run admin-react#build`。
- 优化 `front/apps/admin-react/src/utils/zod.ts` 的 Zod 校验错误处理：隐藏/未注册字段校验失败时也会弹出全局提示，并避免吞掉非 Zod 运行时异常。
- 为后端 `validator` 注册 `notblank` 自定义 tag，支持 string 和 *string 的 trim 后非空校验；SysUser 创建/更新改用 tag 校验，移除重复的 logic 层手写校验函数，并补充 validator 单测。
- 将 logic 包内复用的 `trimStringPtr` 提取为 `go-common/utils/str.TrimStringPtr`，并更新任务、角色、用户、资源 API、菜单资源更新请求归一化调用。
- 精简 SysUser 创建请求中 `notblank` 与 `required` 重叠的 tag，仅保留 `notblank`；补充单测确认 `omitempty,notblank` 对 nil 指针会跳过、对空白字符串指针会失败。

- 修复 `backend/admin/internal/router/auth_router/change_query.go` 中 `new(m.Field)` 被 IDE/Go 解析为类型导致的 `Unresolved type 'Username'` 问题，改为泛型 `ptr(value)` 取指针。
- 修复同文件 `jobExecutionChange` 的非法 UTF-8 struct tag，并通过 `backend/admin` 下的 `go test ./...`。

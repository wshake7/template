# 2026-05-01

## 工作日志

- 优化后台通用 tab 切换体验：在 `_app.tsx` 的 PageContainer 内实现页签内容保活，切换到其他 tab 时隐藏而不卸载页面组件，因此字典、角色、日志等表格页在 10 分钟内切回不会重新刷新；离开某个 tab 超过 10 分钟后再次进入会强制重挂载并刷新数据。
- 将 tab 保活刷新时间提取到 `front/apps/admin-react/src/config/tabs.ts`，统一配置 `TAB_REFRESH_INTERVAL`。
- 为后台字典项接入 `labelComponent`：后端支持模板字段创建、更新、复制和 Swagger/ORM 生成；前端支持 `${EntryLabel}` 模板渲染、预设 Tag 样式、自定义模板、Shiki 高亮编辑和实时预览。
- 优化字典项自定义标签模板编辑体验：将输入区改为 Shiki 高亮编辑块本体，并放宽自定义 Tag 模板解析，保证自定义样式可实时预览。
- 继续优化字典项自定义模板：预览支持受控 `Tag/span/div` 单元素模板，输入高亮改为延迟刷新以降低键入卡顿。

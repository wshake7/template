# 2026-05-10

- 修复 admin-react 动态菜单未按 `order` 排序的问题：`_app.tsx` 在后端菜单树转换为 `ProLayout` 菜单前，对每层节点按 `order` 升序排序，并用 `id` 作为同权重兜底；验证 `vp staged`、`vp run admin-react#build` 通过。

- 实现 `SysResourceApi` 前后端管理功能：启用 ORM 模型并生成 query，新增 `/api/sys/resource/api/*` 后端 CRUD、Swagger 文档和系统管理菜单种子数据。
- API 资源路径模板支持动态参数：后端将 `{id}` 归一化为 `:id` 保存，并校验参数名只能包含字母、数字、下划线且不能以数字开头；补充单测覆盖路径归一化和非法参数。
- 新增 admin-react “API资源”管理页：使用 `usePagination` + `ProTable` + 右侧抽屉表单，支持路径/模块搜索、方法筛选、状态筛选、创建、编辑和删除。
- 验证：`make script-orm`、`make swagger`、`go fix ./...`、`go vet ./...`、`go test ./...`、`vp staged`、`vp run admin-react#build`、`vp run lint` 通过；`vp run lint` 仅保留既有 `api.log.tsx` 两条 warning；浏览器冒烟确认 `/system/resource/api` 页面和创建抽屉可渲染且无控制台错误。

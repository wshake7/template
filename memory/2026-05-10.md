# 2026-05-10

- 修复 admin-react 动态菜单未按 `order` 排序的问题，并验证 `vp staged`、`vp run admin-react#build` 通过。
- 继续修正左侧菜单与菜单管理排序不一致：后端 `/api/sys/resource/menu/tree` 返回前按 `sortOrder` 排序，前端动态菜单优先使用 `sortOrder`，并验证 `make swagger`、`go fix ./...`、`go vet ./...`、`go test ./...`、`vp run admin-react#build` 通过。
- 调整菜单管理“排序”输入框，允许负数，避免 Ant Design 在输入负值时报错，并验证 `vp run admin-react#build` 通过。
- 实现 `SysResourceApi` 前后端管理：补齐 ORM/query、`/api/sys/resource/api/*` 后端 CRUD、Swagger 文档和系统管理菜单种子数据；补充路径模板归一化与校验测试；前端新增 API 资源管理页，并完成相关构建与 lint 验证。
- 实现 `SysUser` 后端 CRUD：新增 `backend/admin/internal/router/logic/sys_user.go` 与 `/api/sys/user/list|create|update|del` 路由，创建时服务端加密密码，更新接口仅处理基础字段，不纳入角色关系。
- 补充 `backend/admin/internal/router/logic/sys_user_test.go`，使用 SQLite 临时库覆盖密码加密、用户名唯一、更新保留未传字段、批量软删除和默认 `id desc` 排序。
- 验证 `go fix ./...`、`go vet ./...`、`go test ./...`、`make swagger` 通过，Swagger 已生成 `SysUser` 接口定义。
- 接入 `SysUser` 前端管理页：新增 `front/apps/admin-react/src/api/business/sysUser.ts` 与 `front/apps/admin-react/src/routes/_app/system/user.tsx`，支持用户名/昵称搜索、状态筛选、创建、编辑和删除，创建态单独展示初始密码字段。
- 验证 `vp run admin-react#build` 通过；构建同时更新了 `routeTree.gen.ts` 与 `auto-imports.d.ts`，并纳入当前文件系统路由结构。

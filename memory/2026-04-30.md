# 2026-04-30

## 工作日志

- 梳理 template 仓库结构与技术栈，按可复用工作流整理项目级技能。
- 新增 `.agents/skills/` 分层索引与技能：项目导航、前端管理后台、后端 admin 服务、orm-crud、go-common、记忆维护。
- 删除 `project/orientation` 和 `operations/memory-maintenance` 两个元技能，保留根目录 `AGENTS.md` 作为身份与记忆规则入口。

## 决策

- 技能按 `project/`、`frontend/`、`backend/`、`operations/` 分层，避免单个技能过大，后续可按任务范围精确读取。
- 技能库后续只保留具体项目能力；身份、项目导航和收尾规则继续由根目录 `AGENTS.md` / `MEMORY.md` 承担。

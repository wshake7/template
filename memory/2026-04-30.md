# 2026-04-30

## 工作日志

- 调整后台字典项数据权限：移除 `sys_dict_entry` 数据权限依赖，字典项列表、更新、删除、复制统一通过所属 `sys_dict_type` 的读写删权限判断。
- 优化后台字典类型分页权限查询：同一次请求中复用 `sys_dict_type` 的 read/write/delete 权限行，write/delete 表达式一致时复用可操作 ID 结果，减少重复 SQL。
- 梳理 template 仓库结构与技术栈，按可复用工作流整理项目级技能。
- 新增 `.agents/skills/` 分层索引与技能：项目导航、前端管理后台、后端 admin 服务、orm-crud、go-common、记忆维护。
- 删除 `project/orientation` 和 `operations/memory-maintenance` 两个元技能，保留根目录 `AGENTS.md` 作为身份与记忆规则入口。
- 新增 `.github/workflows/ai-skill-optimizer.yml` 与 `.github/scripts/optimize_skills.py`，当 `backend/**` 或 `front/**` 提交变更时，默认用 DeepSeek 分析 diff 并把可复用经验追加到对应项目技能。
- 完善后台字典模块 update / del 的数据权限过滤：字典类型和字典项更新、删除按 `sys_data_permission` 的 write/delete 动作收敛可操作数据范围。
- 按生成约定调整字典数据权限实现：`query.WithDBScopes` 移入 `single_vars.go.tpl` 及生成文件，通用数据权限查询构造下沉到 `data_permission` 包。
- 整理 `data_permission` 包：拆分读过滤、写删查询构造、主体构造和权限行解析文件，优化方法命名并补充注释。
- 字典删除请求 ID 去重改为复用 `go-common/utils/slices_utils.Distinct`，移除 `data_permission` 内部专用去重方法。
- 字典模块数据权限资源表名改为调用 `models.SysDictType/TableName()` 与 `models.SysDictEntry/TableName()`，避免在路由逻辑中硬编码表名。
- 数据权限 action 新增 `all` 语义：`["all"]` 同时匹配 read/write/delete；初始化 SQL 调整为 root 拥有全部 `sys_dict_type` 权限，其他用户拥有排除 `system:is_enabled` 外的全部权限。
- 重新拆分字典数据权限种子：root 可操作所有字典类型/字典项，非 root 可读取全部但写入/删除时排除 `system:is_enabled`；字典列表和字典项创建/复制补充数据权限校验。
- 字典类型写删排除条件改为 `id__not: 1`，与字典项 `sysDictTypeId__not: 1` 规则保持一致；前端新增字典项按钮跟随所选字典类型 `canWrite` 禁用。

## 决策

- 技能按 `frontend/`、`backend/` 分层，避免单个技能过大，后续可按任务范围精确读取。
- 技能库后续只保留具体项目能力；身份、项目导航和收尾规则继续由根目录 `AGENTS.md` / `MEMORY.md` 承担。
- AI 技能优化器采用 provider 抽象：workflow 默认配置 `AI_PROVIDER=deepseek`，后续切换服务商只需在脚本中新增 provider 并调整 workflow 环境变量。

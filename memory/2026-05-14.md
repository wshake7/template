# 2026-05-14

- 将请求上下文内剩余的全局 logger 用法改为上下文 logger：`NonceMiddleware` Redis 错误日志改用 `ctx.L()`，SSE 事件流日志改用 `c.L()`；目标目录 `backend/admin/internal/router` 与 `backend/admin/internal/fiberc/middleware` 已无 `zap.L()` / `zap.S()` 命中。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。

- 修复直接 SQL 插入 `job_schedule` 后点启停报 Temporal Schedule not found：`switch` 遇到 Temporal NotFound 时会先按数据库配置同步创建 Schedule，再执行 pause/unpause。验证：`go test ./...` 通过。

- 新增 Temporal 示例业务 Workflow `PrintCountWorkflow`：接收 `count` 参数并在 worker 日志打印，注册到当前 admin Temporal worker；给用户提供每 10 秒执行一次的 `job_schedule` insert SQL 示例。验证：`go test ./...` 通过。

- 将 `job_schedule`、`job_execution` 两张 Temporal 任务调度相关表转换为 GORM model，新增 `JobSchedule`、`JobExecution`，包含状态/类型常量、JSON 字段、时间字段、索引与表名映射。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。

- 按现有 Fiber service 规范接入 Temporal Go SDK：新增 `TemporalConfig`、`services/temporalc` 客户端封装、`services.Temporal` 生命周期服务，默认禁用，启用后初始化 Temporal client / ScheduleClient，并支持可选 worker。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。

- 修复 SSE 导致后台优雅关闭一直等待的问题：新增 `internal/lifecycle` 关闭信号，Fiber `OnPreShutdown` 广播关闭状态，`/api/events` 的 SSE 写循环监听该信号并立即返回；同时将服务器关闭改为 `ShutdownWithTimeout(5s)`，避免长连接无限阻塞关闭流程。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。
- 完善 Job Schedule / Job Execution 前后端逻辑：新增 Temporal 调度包装 Workflow、执行记录 Activity、任务配置/执行记录后端接口与 auth 路由，补充初始化菜单/API/Casbin/root 权限种子数据；前端新增 `/job/schedule`、`/job/execution` 页面及 API 封装。验证：`go test ./...`、`go fix ./...`、`go vet ./...`、`make swagger`、`vp run admin-react#build`、`vp staged` 已执行；dev server 运行在 `http://localhost:3004/`，未登录访问新路由会跳转 `/login`。
- 调整 `front/apps/admin-react/src/routes/_app/job/schedule.tsx` 的启用/停用展示：使用 `useDictMatch(DictCode.SYS_IS_ENABLED_DICT_CODE)` 渲染状态下拉、表格状态和启停操作文案，并在前端 admin-react skill 中记录“字典型字段优先使用 dict”的约定。

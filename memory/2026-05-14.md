# 2026-05-14

- 修复 SSE 导致后台优雅关闭一直等待的问题：新增 `internal/lifecycle` 关闭信号，Fiber `OnPreShutdown` 广播关闭状态，`/api/events` 的 SSE 写循环监听该信号并立即返回；同时将服务器关闭改为 `ShutdownWithTimeout(5s)`，避免长连接无限阻塞关闭流程。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。

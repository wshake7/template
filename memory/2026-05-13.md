# 2026-05-13

- 完善登录日志全链路：修正 `SysLoginLog` 模型字段与索引，登录成功/失败异步写入 `sys_login_log`，新增 `/api/sys/login/log/list` 与 `/detail` 后端接口、Swagger、初始化菜单/API/Casbin/root 授权数据、结构加固脚本和前端“登录日志”管理页；补充 nil `sys_user_id` 模型测试。验证：`make script-orm`、`make swagger`、`go fix ./...`、`go vet ./...`、`go test ./...`、`vp run admin-react#build` 通过；`vp staged` 因无 staged 文件未执行 lint-staged。

- 调整 `backend/admin/cmd/scripts/init.sql` 中顶级菜单目录排序：系统管理 `sort_order/order=1`，账号管理 `sort_order/order=2`，日志信息 `sort_order/order=3`。
- 完善 API 权限与 Casbin 策略同步：角色保存授权时按 API 差异增删 `casbin_rule`，角色 code/启停变化时重建该角色策略，API 资源关闭/删除/路径或方法变化时删除旧策略，开启或变更后为已绑定角色重建策略；Casbin 启动时显式开启 auto-save。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。
- 在 `backend/admin/cmd/scripts/init.sql` 增加 `casbin_rule` 初始化数据：参考 `sys_casbin_model` 的 PBAC 表达式和 41 条 `sys_resource_api` 资源，为 `role:root` 生成对应 API 策略。验证：检查新增 41 条 `keyMatch2` 策略。
- 资源 API 页的请求方法展示改为使用 `DictCode.SYS_RESOURCE_API_TYPE_DICT_CODE`：移除硬编码 `methodOptions/methodColor`，列表方法列和筛选/表单选项均从 `useDictMatch` 获取；同步将初始化 SQL 中 API 方法字典编码对齐为 `system:resource_api_type`。验证：定向 `eslint`、`vp run admin-react#build` 通过。
- 在 `backend/admin/cmd/scripts/init.sql` 增加 HTTP 方法字典初始化数据：新增 `system:resource_api_type` 字典类型及 `GET/POST/PUT/PATCH/DELETE/OPTIONS/HEAD` 七个字典项，保留前端原有颜色配置到 `label_component`。
- 资源菜单页的菜单类型展示改为使用 `DictCode.SYS_RESOURCE_MENU_TYPE_DICT_CODE`：移除硬编码 `menuTypeLabel/menuTypeColor/menuTypeOptions`，列表类型列和表单分段选项均从 `useDictMatch` 获取，保留枚举 fallback。验证：定向 `eslint`、`vp run admin-react#build` 通过。
- 在 `backend/admin/cmd/scripts/init.sql` 增加菜单类型字典初始化数据：新增 `system:resource_menu_type` 字典类型及 `CATALOG/MENU/BUTTON/EMBEDDED/LINK` 五个字典项，对应“目录/菜单/按钮/内嵌/外链”。
- 按反馈移除前端 `isEnabled` 字典渲染外层手写 `Tag`，状态列直接返回 `enabledStatus.renderLabel(...)`，由字典项自身控制展示组件。验证：定向 `eslint`、`vp run admin-react#build` 通过。
- 统一前端 `isEnabled` 状态展示的字典来源：角色、用户、API 资源、菜单资源、字典面板和语言面板的状态标签、筛选项、开关文案与启停操作文案均改为通过 `useDictMatch(DictCode.SYS_ENABLED_STATUS_DICT_CODE)` 获取。验证：`vp run admin-react#build`、定向 `eslint` 通过。

- 优化 `front/apps/admin-react/src/hooks/useDictMatch.tsx`：`useDictMatches` 批量获取现在按字典码返回与 `useDictMatch` 一致的 `entries/getEntry/getLabel/renderLabel` 使用体验，并复用同一套缓存与渲染逻辑。验证：`vp run admin-react#build` 通过。
- 修复 `useDictMatch.tsx` 中 `useDictMatches` 直接调用 `useTranslation` 导致的 `NO_I18NEXT_INSTANCE` 警告：入口显式加载 `src/i18n.ts`，hook 改为使用项目 i18n 实例并订阅语言变化。验证：`pnpm.cmd --dir front --filter admin-react exec eslint src/hooks/useDictMatch.tsx src/main.tsx`、`vp run admin-react#build` 通过。

- 补充 `backend/admin/cmd/scripts/init.sql` 中 auth_router 对应的 API 初始化数据：新增 41 条 `sys_resource_api` 种子记录，并为 root 角色新增 41 条 `sys_role_api` 授权记录。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过，其中 `go test` 首次因沙箱无法访问 Go build cache 失败，提权重跑通过。

- 每日代码检测：修复 `backend/go-common/utils/id/machineid` Windows 测试兼容性问题；生成 `docs/code_task/task.md`。验证 `pnpm.cmd exec eslint .`（0 errors/2 warnings）、`pnpm.cmd run build`、`pnpm.cmd run -r test`、各 Go 模块 `go vet ./...` / `go test ./...` 通过。
- 实施代码检测 3 个待处理项：抽出 API 日志 `JsonCodeBlock` 组件，集中 Shiki HTML 安全边界；重构高亮异步 state，消除 hook lint 警告；在任务报告中明确 Windows PowerShell 使用 `pnpm.cmd`。验证 `pnpm.cmd exec eslint .`、`pnpm.cmd run build`、定向 Playwright CT `jsonCodeBlock.spec.tsx` 通过；全量 `admin-react e2e:test` 仅因既有示例依赖本地 3000 服务和外网访问失败。
- 根据反馈修正 auto-import 处理方式：在 `front/apps/admin-react/vite.config.ts` 的 `AutoImport` 配置中使用 `dirsScanOptions.fileFilter` 排除 `src/components/business/logger/`，并设置 `dtsMode: 'overwrite'`，确保 `JsonCodeBlock` 和内部 `formatJsonContent` 不会在后续构建中重新进入 `auto-imports.d.ts`。
- 为 `front/apps/admin-react/src/routes/_app/system/resource.api.tsx` 添加 API 资源列表行内启用/停用操作：复用 `apiUpdate` 切换 `isEnabled`，并在操作列按 `canWrite` 控制按钮可用性。验证：`vp staged` 通过。
- 为 `front/apps/admin-react/src/routes/_app/account/role.tsx` 角色授权抽屉添加 API 权限本地搜索：支持按模块、方法、路径筛选，并保留搜索隐藏项的已选状态。验证：`vp run admin-react#build` 通过。
- 为 `front/apps/admin-react/src/routes/_app/account/role.tsx` 角色授权抽屉的 API 权限表格补充备注列，空备注显示 `-`，长文本省略。验证：`vp run admin-react#build` 通过。
- 将 `front/apps/admin-react/src/routes/_app/account/role.tsx` 角色列表改为树形展示：使用 `noPaging` 树数据，增加展开/折叠全部按钮和 `expandable` 控制，避免分页拆散父子角色。验证：定向 ESLint 通过，`vp run admin-react#build` 通过；`vp staged` 因无 staged 文件未执行实际检查。
- 为角色列表操作栏补充行内“新增”按钮：点击当前角色行可创建子角色并自动带入父级角色；顶部创建按钮保持创建根角色。验证：定向 ESLint 通过，`vp run admin-react#build` 通过。
- 检查并优化 `backend/admin/internal/fiberc/middleware/casbin.go`：修正 `ctx.Matched()` 误用为 `ctx.Method()`，补充 Session/Casbin 初始化保护、Enforce 错误日志、空角色过滤和未授权响应；认证路由链挂载 Casbin API 鉴权中间件。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。
- 修复 Casbin `eval()` 在无策略时 `please make sure rule exists in policy` 报错：中间件将该旧模型错误按未授权处理，同时把 PBAC 模型、初始化 SQL 和策略生成从 `eval(p.sub_rule)/eval(p.obj_rule)` 改为普通 `sub/obj/act + keyMatch2`；启动时兼容旧数据库模型并自动规范旧策略。验证：`go fix ./...`、`go vet ./...`、`go test ./...` 通过。

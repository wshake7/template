# 2026-05-06

## 工作日志

- 将 admin-react 字典项自定义模板编辑器从手动组合 `monaco-editor-core` + `@shikijs/monaco` 改为通过 `modern-monaco` 初始化，保留 HTML 语言和 GitHub Light 主题配置；同步移除直接依赖并更新 Monaco vendor 分包匹配。
- 已通过 `vp run admin-react#build` 验证；`vp staged` 执行成功，当前没有 staged 文件可处理。
- 修复 `useDictMatch` 中对 `i18n.exists` 的运行时依赖，改用 `t(entryLabel, { defaultValue: entryLabel })` 回退字典原标签，避免字典列渲染触发 ErrorBoundary；已通过 `vp run admin-react#build` 验证，`vp staged` 返回当前无 staged 文件。
- 优化 `useDictMatch` / `useDictMatches` 的字典匹配请求：因 alova `cacheFor` 不能消除首屏并发重复请求，已将 `entryMatch` 还原为 `cacheFor: 0`，改用 hook 内模块级缓存、订阅快照和微任务批量队列合并缺失 code；已通过 `vp run admin-react#build` 验证，`vp staged` 返回当前无 staged 文件。
- 修复 `sys_dict_entry` 自动建表外键约束：将字典/语言类型反向关联的 `OnDelete:SET NULL` 调整为 `RESTRICT`，与子表 `not null` 外键一致，并修正 `language_code` 的 GORM tag 默认值分隔符；已通过 `go fix ./...`、`go vet ./...`、`go test ./...` 验证。

- ʵʩϵͳ���ṹ�����޸���ģ�͸���δɾ������ partial unique index��`sys_data_permission` ���� `action_key` �淶���ͱ���ǰУ�飬`SysUser.Password` ���� JSON �����API ��־д��ǰ�������ضϣ����� PostgreSQL �ӹ�Ǩ�ƽű�����ص�Ԫ���ԣ���ͨ�� `make script-orm`��`go fix ./...`��`go vet ./...`��`go test ./...`��

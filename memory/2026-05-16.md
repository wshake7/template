# 2026-05-16

- Fixed three admin-react ESLint hook warnings:
  - Removed derived dynamic menu state from `_app.tsx`; menu items now derive from `dynamicMenuTree`.
  - Stabilized job execution table callbacks with `useCallback`.
  - Stabilized job schedule edit/action callbacks and completed `useMemo` dependencies.
- Verified targeted files with `pnpm.cmd exec eslint`.
- Fixed `app-react-ssr` React Start router entry cycle:
  - Changed `src/router.ts` to export `getRouter()` as a per-call router factory.
  - Removed `~/router` singleton imports from root route and API login flow.
  - Verified targeted files with ESLint; full `tsc` is still blocked by existing React Start subpath type resolution errors in `client.tsx`/`server.ts`.

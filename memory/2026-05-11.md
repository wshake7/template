# 2026-05-11

- 移除 `SysUser` 的 `LastLoginAt`、`LastLoginIP` 字段，并同步清理后端 ORM model、GORM Gen 生成代码、Swagger 文档、前端 `sysUser` 类型与用户列表展示列。
- 更新 `backend/admin/cmd/scripts/init.sql`，去除 `sys_user` 初始化数据中的 `last_login_at`、`last_login_ip` 列和值。
- 更新 `backend/admin/cmd/scripts/schema_hardening_postgres.sql`，为已有 PostgreSQL 库补充 `DROP COLUMN IF EXISTS "last_login_at"` 和 `DROP COLUMN IF EXISTS "last_login_ip"` 迁移动作。
- 验证 `make script-orm`、`make swagger`、`go fix ./...`、`go vet ./...`、`go test ./...` 通过；`vp staged` 因 `front/vite.config.ts` 缺少 `staged` 配置而无法执行。

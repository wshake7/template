# 2026-05-05

- 将字典 entry match 接口从单个 code 扩展为批量 codes 匹配，返回按字典编码分组的启用字典项；同步更新 admin-react 字典 API 类型与 useDictMatch/useDictMatches hook。

## 工作日志

- 优化 admin-react 操作日志详情弹窗：限制弹窗内容区高度并允许内部滚动，长 URL、User-Agent 和 JSON 字段自动换行，避免详情内容横向溢出。
- 将操作日志请求体、请求头、响应信息改为格式化 JSON 展示，并通过 Shiki core 按需加载 JSON 语言与 GitHub Light 主题进行高亮，避免引入 Shiki 全量包导致 PWA 构建失败。
- 调整操作日志详情弹窗宽度与 Descriptions 标签列宽，让内容区域获得更多横向空间。
- 将项目中的 SysOperationLog 统一重命名为 SysApiLog，同步后端模型、GORM query、路由、Swagger 和前端 API/页面路由，并改为通过 SysUser 关联显示操作人。
- 将 SysApiLog 的用户关联从 UserID 改为可空 SysUserID/sys_user_id，未登录或系统请求时写入 NULL，避免 user_id=0 导致外键约束失败。
- 将 API日志页面列表请求改为复用 ApiLogApi.list，与其他页面的 usePagination API 调用方式保持一致。
- 优化 SysApiLog 写入的用户关联：移除模型上的数据库外键约束声明，并在旧库触发外键错误时将 SysUserID 置空重试写日志。
- 优化 SysApiLog 详情查询，Preload SysUser 时仅选择 id 和 username，避免返回不必要的用户字段。
- 将 SysApiLog 详情的 SysUser 预加载改为 gorm/gen 强类型写法，仅查询用户 id 和 username。
- 修复 API日志列表操作者不显示 username：分页查询也预加载 SysUser，并仅选择用户 id 和 username。

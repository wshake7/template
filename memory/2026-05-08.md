# 2026-05-08

- 移除 SysResource 模块相关代码：后端删除 `/api/sys/resource/*` 路由、处理逻辑、ORM model、过期 query 生成文件和 Swagger 引用；前端删除资源管理页面、业务 API、路由生成引用和自动导入声明。
- 保留 `sys_data_permission.resource_table` 等数据权限通用资源字段，仅移除 `sys_resource` 表自身的索引硬化语句。
- 验证：`make script-orm`、`make swagger`、`go fix ./...`、`go vet ./...`、`go test ./...`、`vp run admin-react#build` 通过；`vp staged` 因没有 staged 文件未执行 lint-staged。
- 基于 `SysResourceMenu` 重新实现动态菜单资源：启用模型生成链路，新增 `/api/sys/resource/menu/list|tree|create|update|del`，前端新增菜单资源树表管理页，并让后台侧栏优先加载后端动态菜单、静态路由兜底。
- 调整计划中的启停设计：不新增 `/switch` 接口，表格启停直接调用 `/api/sys/resource/menu/update` 更新 `isEnabled`。
- 验证：`make script-orm`、`make swagger`、`go fix ./...`、`go vet ./...`、`go test ./...`、`vp run admin-react#build`、`vp staged` 通过；前端构建有既有大 chunk 警告。

# 2026-05-09

- 修复资源菜单页刷新时报 ZodError：基础表单 schema 允许默认空标题，标题必填改到提交 schema 的 `superRefine` 中校验，并补充到 admin-react 技能说明。
- 统一后台表单类型写法：资源菜单页移除手写 `FormValues` interface，改用 `z.infer<typeof Schema>`；默认值和编辑回显通过 `Schema.partial().parse(...)` 转换后再 `Schema.parse(...)` 归一化，并同步更新 admin-react 技能说明。
- 将管理后台列表分页和表单约定沉淀到 `.agents/skills/frontend/admin-react/SKILL.md`：分页优先使用 alova `usePagination`，表单优先使用 zod schema + `useZodForm`。
- 调整资源菜单页实现风格：列表分页改为 alova `usePagination` 驱动，父级菜单选项单独加载完整树；编辑抽屉表单改为 zod schema + `useZodForm` 统一校验和提交。

- 优化 SysResourceMenu 菜单管理体验：树表增加标题/状态筛选、展开/折叠工具栏，创建/编辑改为右侧抽屉。
- 按菜单类型调整表单字段：目录/菜单使用 `icon/order/hidden`，按钮/内嵌/外链使用 `authorities`；后端同步规范化并在动态菜单树返回 `authorities`。
- 继续优化菜单图标选择：新增 Ant Design 图标工具，资源菜单表单支持按线框/实底/双色三种风格切换，并以可滚动图标网格选择图标；动态菜单将保存的图标名渲染为 Ant 图标组件。
- 将 Ant 图标选择器提取为 `components/common/antIconPicker.tsx` 公共组件，增加图标搜索，并通过虚拟滚动减少创建菜单抽屉打开时的一次性渲染开销。
- 验证：`go fix ./...`、`go vet ./...`、`go test ./...`、`make swagger`、`vp run lint:fix`、`vp run admin-react#build` 通过；前端构建仍有既有大 chunk 警告。
- 修复并优化资源菜单编辑抽屉数据回显：点击编辑时先同步表单值再打开抽屉，并用 `forceRender` 预挂载表单，避免等 Drawer 打开动画结束才显示数据；验证 `vp staged`、`vp run admin-react#build` 通过。
- 优化资源菜单上级菜单选择规则：目录只能挂根节点或目录，菜单只能挂目录，按钮/内嵌/外链只能挂菜单；前端过滤父级选项并校验，后端创建/更新同步校验父级类型。
- 修复资源菜单编辑时上级菜单不回显：后端模型补充 `parentID/treePath` JSON 标签，前端按统一的小驼峰 `parentID` 字段读取。
- 优化资源菜单编辑回显：移除表单整体 `preserve={false}`，避免先编辑目录再编辑菜单时条件卸载的 `component` 字段被清空。
- 优化管理后台刷新时侧边栏图标延迟出现：静态路由菜单补充首屏图标，动态菜单树改由 zustand `persist` store 缓存，刷新时先用缓存同步渲染，再异步请求后端更新。
